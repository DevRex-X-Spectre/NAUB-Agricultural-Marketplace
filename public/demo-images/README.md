# Demo images

Local product photos for seed listings. Sourced from **Unsplash** (royalty-free).

Populate with:

```bash
node scripts/fetch-demo-images.mjs
```

Do not hotlink external URLs in `lib/seed.ts` — always reference `/demo-images/...` paths.
